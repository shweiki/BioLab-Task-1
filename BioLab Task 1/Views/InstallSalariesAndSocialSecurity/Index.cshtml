@model List<POCO.SalaryBasicInformation>

<h2>Install Salaries && Socia lSecurity</h2>
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                @{ Html.RenderPartial("SearchForm");}

            </div>
        </div>
    </div>
    <!-- Column -->
    <div class="card">
        <div class="card-body">
            <div class="row" id="DisplayAreaMain">
                @{ Html.RenderPartial("SalaryTable", Model);}
            </div>
          
        </div>
    </div>
</div>

